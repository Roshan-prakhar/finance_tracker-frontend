[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every unknown route should serve index.html so React Router can take over
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
